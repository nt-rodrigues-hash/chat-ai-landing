# React Component Structure
## Functional vs Class Components
- Functional components use hooks
- Class components use `this.state` and lifecycle methods

## Component Lifecycle
- `useEffect` replaces componentDidMount/Unmount
- `useCallback` and `useMemo` optimize rendering

## PropTypes
- Validate prop types with `prop-types` library
- Enforce type safety in components